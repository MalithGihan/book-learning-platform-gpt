# name: Backend Deployment

# on:
#   push:
#     branches: [ production ]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm
#           cache-dependency-path: package-lock.json

#       - name: Install deps (workspaces)
#         run: npm ci --no-audit --no-fund

#       - name: Build server
#         run: npm run build -w server

#       - name: Install prod deps inside server (so Lambda package includes node_modules)
#         working-directory: server
#         run: |
#           rm -rf node_modules
#           npm install --omit=dev --no-audit --no-fund --workspaces=false

#       - name: Configure AWS creds
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ secrets.AWS_REGION }}

#       - name: Deploy (Serverless)
#         working-directory: server
#         env:
#           CLIENT_ORIGINS: ${{ secrets.CLIENT_ORIGINS }}
#           MONGO_URI: ${{ secrets.MONGO_URI }}

#           AI_API_KEY: ${{ secrets.AI_API_KEY }}
#           AI_BASE_URL: ${{ secrets.AI_BASE_URL }}
#           AI_MODEL: ${{ secrets.AI_MODEL }}
#           AI_REQUEST_LIMIT: ${{ secrets.AI_REQUEST_LIMIT }}

#           JWT_ACCESS_SECRET: ${{ secrets.JWT_ACCESS_SECRET }}
#           JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET }}
#           ACCESS_MINUTES: ${{ secrets.ACCESS_MINUTES }}
#           REFRESH_DAYS: ${{ secrets.REFRESH_DAYS }}

#           COOKIE_SECURE: ${{ secrets.COOKIE_SECURE }}
#           COOKIE_SAMESITE: ${{ secrets.COOKIE_SAMESITE }}
#         run: npx serverless@3 deploy --force
